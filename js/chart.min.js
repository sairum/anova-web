
var chart=(function(){'use strict';const POINTS=61,PRECISION=4;var Average=5.0,Stdev=0.5;var Xmin,Xmax,Xrange,Xpunit;var xtitle="X",ytitle="Probability density";var margin={top:10,left:70,right:10,bottom:50};var points=[];var canvas=document.getElementById("chart");var cHeight=canvas.height;var cWidth=canvas.width;var ctx=canvas.getContext("2d");var xsize=cWidth-(margin.right+margin.left);var ysize=cHeight-(margin.bottom+margin.top);var xmin=margin.left,xmax=cWidth-margin.right,ymin=margin.top,ymax=ymin+ysize;var xrange=xmax-xmin,yrange=ymax-ymin;function getX(x){let r=Xpunit*parseFloat(x-Xmin);if(r<0)return xmin;if(r>xmax)return xmax;return Math.round(xmin+r);}
function generateData(){let x,y,X,Y,maxYValue=0;points=[];let Xstep=Xrange/POINTS;let xstep=Math.round(xrange/POINTS);let ystep=Math.round(yrange/10);for(let i=0;i<POINTS;i++){X=Xmin+i*Xstep;x=getX(X);Y=jStat.normal.pdf(X,Average,Stdev);y=ymax-Math.round(Y*yrange);if(Y>maxYValue)maxYValue=Y;points.push({x:x,y:y,X:X,Y:Y});}
points.maxYvalue=maxYValue;if(maxYValue>0.9999){for(let i=0;i<POINTS;i++){points[i].y=ymax-Math.round(points[i].Y/maxYValue*yrange);}}}
function render(){Xmin=parseFloat(Average-PRECISION*Stdev),Xmax=parseFloat(Average+PRECISION*Stdev);Xrange=parseFloat(Xmax-Xmin);Xpunit=parseFloat(xrange/Xrange);generateData();ctx.fillStyle='white';ctx.fillRect(0,0,cWidth,cHeight);ctx.fillStyle='white';ctx.fillRect(margin.left,margin.top,xsize,ysize);ctx.fillStyle='black';ctx.font='16pt Arial';ctx.textAlign="center";let txtSize=ctx.measureText(xtitle);ctx.textAlign='center';ctx.fillText(xtitle,margin.left+(xsize/2),cHeight-(margin.bottom/4));ctx.save();ctx.rotate(-Math.PI/2);ctx.fillText(ytitle,(ysize/2)* -1,margin.left/3);ctx.restore();ctx.strokeStyle='black';ctx.lineWidth=1;ctx.beginPath();ctx.moveTo(margin.left,cHeight-margin.bottom);ctx.lineTo(cWidth-margin.right,cHeight-margin.bottom);ctx.stroke();ctx.closePath();ctx.beginPath();ctx.moveTo(margin.left,margin.top);ctx.lineTo(margin.left,cHeight-margin.bottom);ctx.stroke();ctx.closePath();let txt;ctx.font='10pt Calibri';ctx.textAlign="end";ctx.textBaseline="middle";const YTICKS=10;let yrange=ymax-ymin;let ystep=Math.round(yrange/YTICKS);let ys=points.maxYvalue/YTICKS;let y;for(let i=0;i<=YTICKS;i++){if(ys<0.1)txt=(i/YTICKS).toFixed(2).toString();else txt=(i*ys).toFixed(2).toString();y=ymax-i*ystep;ctx.beginPath();ctx.moveTo(xmin,y);ctx.lineTo(xmin-5,y);ctx.stroke();ctx.closePath();ctx.fillText(txt,xmin-6,y);}
const XTICKS=10;let dec_places=1;if(Stdev<1)dec_places=2;if(Stdev<0.1)dec_places=3;if(Stdev<0.01)dec_places=4;if(Stdev<0.001)dec_places=5;let xr=parseFloat(Xrange/XTICKS);let xScaleStep=Math.round(xrange/XTICKS);let x;ctx.textAlign='center';for(let i=1;i<XTICKS;i++){x=xmin+i*xScaleStep;txt=(Xmin+i*xr).toFixed(dec_places).toString();ctx.beginPath();ctx.moveTo(x,ymax);ctx.lineTo(x,ymax+5);ctx.stroke();ctx.closePath();ctx.fillText(txt,x,ymax+15);}
ctx.strokeStyle='red';ctx.lineWidth=1;ctx.beginPath();ctx.moveTo(getX(Average),ymax);ctx.lineTo(getX(Average),ymin);ctx.stroke();ctx.closePath();ctx.translate(0.2,0.1);ctx.strokeStyle='blue';ctx.lineWidth=1;ctx.beginPath();ctx.moveTo(points[0].x,points[0].y);for(let i=1;i<POINTS;i++){ctx.lineTo(points[i].x,points[i].y);ctx.stroke();}
ctx.closePath();ctx.translate(-0.2,-0.1);}
function setAverage(m){Average=parseFloat(m);let min=Average-PRECISION*Stdev;let max=Average+PRECISION*Stdev;Xmin=min;document.getElementById("min").value=min;Xmax=max;document.getElementById("max").value=max;render();}
function setStdev(m){Stdev=parseFloat(m);if(Stdev==0){Stdev=0.1;document.getElementById("stdev").value=Stdev;}
let min=Average-PRECISION*Stdev;let max=Average+PRECISION*Stdev;Xmin=min;document.getElementById("min").value=min;Xmax=max;document.getElementById("max").value=max;render();}
function setXMax(m){Xmax=parseFloat(m);render();}
function setXMin(m){Xmin=parseFloat(m);render();}
function setXtitle(t){xtitle=t;render();}
function setYtitle(t){ytitle=t;render();}
return{render:render,setXMin:setXMin,setXMax:setXMax,setAverage:setAverage,setStdev:setStdev,setXtitle:setXtitle,setYtitle:setYtitle};}());